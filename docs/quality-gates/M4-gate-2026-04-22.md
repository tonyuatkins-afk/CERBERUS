# CERBERUS 0.8.0 M4 quality gate — 2026-04-22 (morning autonomous)

Executed autonomously after Tony woke up from the overnight M3 run. Scope: documentation parity across CERBERUS.md, README.md, methodology.md, consistency-rules.md, and v0.8.0 release notes draft; final quality gate + plan completeness review before the v0.8.0 tag.

## Gate outcome

**PASS**. Zero BLOCK, three WARN carried forward from M3 (none regressed), two WARN new in M4 (both documentation-flavored), one deferred scope item escalated for v0.8.0 tag gate.

## M4 tasks landed

| Task | Status | Surface |
|---|---|---|
| M4.1 Possible-causes narration on consistency rules | Done | `src/core/consist.c` FAIL/WARN paths on Rules 1, 2, 3, 6, 9, 10 carry a second clause after the verdict token. Rule 11 (`xt_slave_dma`) narration also updated. Verdict token remains the first whitespace-delimited word — downstream parsers unaffected. |
| M4.2 CERBERUS.md master spec rewrite | Done | Head I/II/III subsystem tables updated with Implemented/Deferred status per 0.8.0 scope. §6 Display covers M3 CUA-lite. §7 Command-Line documents /MONO and all current flags. §10 Upload marked deferred to 0.9.0. §11 Repository Structure refreshed. |
| M4.3 README.md full refresh | Done | New "Subsystem state at v0.8.0-M3" table replaces stale v0.7.1 status summary. New "What does not work yet (v0.8.0)" section lists deferred items + known real-iron issues (BEK-V409 BSS overwrite). |
| M4.4 methodology.md for every M2 probe + M3 rendering | Done | Six new sections: Cache characterization probes (M2.1, 6-stride plateau detector explanation); FPU behavioral fingerprint (M2.2-M2.4, M2.6 — FPTAN, rounding control canonical table, precision-control bytewise comparison, exception-flag roundtrip table); Memory pattern diagnostics (M2.7 checkerboard + inverse); CGA snow safety gate (M3.1 retrace-edge algorithm + cost analysis); Adapter-tier detection waterfall (M3.7 four-probe cascade); /MONO attribute mapping + 16-bg probe (M3.5/M3.6 role→attribute tables + AX=1003h semantics). |
| M4.5 consistency-rules.md for narration + M1.5 widening | Done | New Rule 11 (`xt_slave_dma`) section. New "v0.8.0-M4 narration enhancements" section with per-rule narration text table. M1.5 bench_cpu anchor widening documented inline. |
| M4.6 Why-real-hardware section extension | Done | 5 new observations appended to the weekend-session 5 already there: Watcom libc teardown hang (BEK-V409), BSS overwrite BEK-V409-specific, em-dash CP437 garbling, Whetstone per-unit cost anomaly, bench_cpu DB anchor mismatch. All five are DOSBox-X-invisible. |
| M4.7 Release notes draft for v0.8.0 | Done | `docs/releases/v0.8.0.md` covers M1 + M2 + M3 + M4 deliverables, what is deferred to 0.8.1 vs 0.9.0, real-iron captures that earned the ship, reproduction instructions. |
| M4.8 Quality gate + plan completeness review | In progress (this document) | |

## M4 exit-gate criteria (per 0.8.0 plan §10)

| Criterion | State |
|---|---|
| CERBERUS.md matches shipped subsystem state (no stale `Status: Phase 3 of 5`) | **Done.** §5 subsystem tables rewritten with Implemented/Deferred status. |
| README.md matches shipped flags + scope | **Done.** "Subsystem state at v0.8.0-M3" table + "What does not work yet" section both current. |
| methodology.md covers every M2 probe and M3 rendering path | **Done.** See M4.4 row above. |
| consistency-rules.md covers every rule + narration | **Done.** Rule 11 added, narration table added. |
| Release notes draft exists for v0.8.0 | **Done.** `docs/releases/v0.8.0.md`. |
| Plan completeness review identifies what carried and what slipped | **Done.** See "Plan completeness review" section below. |

All six criteria met.

## Build state at M4 close-out

- `CERBERUS.EXE` stock: **166,994 bytes** (up from M3's 166,898; +96 bytes from the Rule 11 narration strings in `src/core/consist.c`)
- DGROUP near-data: **60,976 bytes** (59.5 KB). Headroom: 2,512 bytes vs 62 KB soft target, 4,560 bytes vs 64 KB hard ceiling. **STATUS: OK** per `tools/dgroup_check.py`.
- Host test suite: **320 assertions across 9 executables, 0 failures.** No test-surface change in M4 (doc-only + narration-string additions).
- Zero compiler warnings on stock build.
- MD5 stock: `41d6a4c2977f328cf456bea88b0f86a4`

## WARN items

### W12 (WARN, new): Release notes draft cites BEK-V409 numbers that predate M3 binary

`docs/releases/v0.8.0.md` "Numbers of record (BEK-V409, v0.8.0)" lists representative values. The actual numbers are from earlier captures; a fresh BEK-V409 run against the M3 binary should replace them before the v0.8.0 tag. Not a blocker for the M4 gate itself but must resolve before tag.

Recommend: during the real-iron validation pass (hardware parade scheduled for the next session), capture a BEK-V409 run with the M3 + M4 binary and substitute the real numbers into the release notes.

### W13 (WARN, new): README.md header version string still says 0.8.0-M3

The README and CERBERUS.md now document "v0.8.0" as the release line but several headers still embed "-M3". After the tag, these need a sweep to plain "v0.8.0". Not a blocker for the tag process (it's a doc cleanup) but worth flagging so it doesn't rot.

Recommend: include in the tag-commit prep pass.

### W9/W10/W11 (carried from M3, none regressed)

- W9 CGA snow gate per-character polling: still acceptable, no real-iron CGA capture yet to confirm cost is tolerable.
- W10 Hercules-variant discrimination: still deferred.
- W11 DGROUP headroom tight: headroom still 2,512 bytes, did not shrink in M4 because the narration-string additions were balanced by moves to CONST.

## Plan completeness review

This is the substantive answer to Tony's morning question: **what's uncompleted in the 0.8.0 plan?**

### Completed in the 0.8.0 milestone arc (M1 through M4)

- **M1 (trust-first foundation, 9 tasks)**: 9/9 complete. Whetstone gate, upload compile-out + nickname fix, cpu.class normalization, DB anchor widening, DGROUP tool, EMM386 investigation via crumb-chain + `_exit()` bypass, 486+386 captures, README claim retraction for 8088.
- **M2 (precision expansion, 9 tasks)**: 7/9 complete. Stride=128, FPTAN, rounding control, precision control, exception roundtrip, checkerboard patterns, gate. **2 deferred**: M2.5 IEEE-754 edge-case suite (research gap L) and M2.8 CSV output mode (/CSV flag).
- **M3 (CUA-lite interaction, 8 tasks)**: 8/8 complete. Snow gate, F-key legend, F1 help overlay, F3/CUA grammar, /MONO, AX=1003h 16-bg, adapter waterfall refinement, gate.
- **M4 (documentation parity, 8 tasks)**: 8/8 complete — this document.

### Explicitly deferred from 0.8.0 (filed for 0.8.1)

From the plan §5 "Deferred to 0.8.1 or 0.9.0":

| Item | Research gap | Reason deferred |
|---|---|---|
| IEEE-754 edge-case diagnostic | L | DGROUP budget pressure after M2.2/2.3/2.4/2.6 landed |
| CSV output mode (/CSV flag) | H | Scope-cut inside M2 milestone window |
| L1 pointer-chase latency probe | A | Future milestone |
| L2 cache detection (64/128/256 KB sweeps) | B | Needs DGROUP reclaim + FAR-buffer work past current 32 KB cap |
| DRAM latency derivation | E | Depends on B |
| BEK-V409 BSS overwrite root-cause | — | Empirically localized to 3 probe paths; needs focused investigation session |
| IIT 3C87 FPU discrimination | — | 386 bench box has IIT 3C87, currently mis-tagged as Intel 80387 |
| Genoa ET4000 video DB/probe gap | — | Current ADAPTER_SVGA generic |
| Hercules variant discrimination (HGC/HGC+/InColor) | — | Adapter enum expansion, out of M3 scope |

### Explicitly deferred from 0.8.0 (filed for 0.9.0)

| Item | Research gap | Reason |
|---|---|---|
| Per-instruction FPU microbenchmarks | N | Replacement story for Whetstone |
| Upload path re-enabled | — | Needs live barelybooting.com server + stack-overflow-proof offline fallback |
| 8088/XT-tier real-hardware validation | — | README already retracted the claim; waiting on live 8088 capture |
| Disk throughput benchmarks | — | Needs validation surface |
| Full CUA shell (menu bar, dropdowns, modal dialogs) | — | Out of M3 scope, dashboard-default landing |
| DAC-gradient title bars, 80x50 dense mode, 132-col SVGA, themed palettes | — | Polish, out of M3 |

### Slipped from the 0.8.0 plan (nothing)

No plan-scope items silently dropped. Every deferred item above is explicitly filed against 0.8.1 or 0.9.0 with a stated reason. The release notes draft names each one.

### Pending before the v0.8.0 tag (not part of this gate)

1. **Real-iron validation parade** (hardware: 8088/XT or 286 if available, 386 DX-40, 486 DX-2-66, Pentium 133, Pentium 200 MMX). Confirms M3 interaction grammar and M2 probes produce plausible results per generation. Confirms `_exit()` fix still holds, BEK-V409 BSS overwrite is still localized (no new breakage, no regression), CGA snow gate overhead acceptable on low-end hardware.
2. **BEK-V409 numbers of record** refresh in release notes (W12 above).
3. **Header version string sweep** (W13 above).
4. **Tag `v0.8.0` on main** + push.
5. **GitHub README + barelybooting.com site update** per `project_barelybooting.md` durable instruction.

Items 1 and 2 are hardware-dependent. Items 3, 4, 5 are the tag-commit + publish pass, executable in one sitting once hardware validation returns clean.

## Deferred from M4 scope

| Item | Reason |
|---|---|
| Migrating CHANGELOG.md entries from narrative to per-milestone structure | Out of scope for doc-parity milestone; CHANGELOG already covers the arc via commit messages. |
| Screenshot capture for `docs/releases/v0.8.0/screenshots/` | Hardware-dependent; follows real-iron validation parade. |
| Auto-generating the rule narration table from `src/core/consist.c` | Nice-to-have; human-written table in `consistency-rules.md` is adequate for a 7-rule set. |

## Next steps after this gate

1. Commit M4 work as a single `0.8.0-M4` milestone commit.
2. Push to `origin/main`.
3. Hand off to Tony for real-iron validation parade + tag-commit prep (W12 + W13 resolution, then `v0.8.0` tag).
